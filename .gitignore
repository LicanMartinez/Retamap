# OS
.DS_Store
Thumbs.db

# Editors
*.swp
.vscode/

# R
.Rhistory

# Experimental / non-canonical variants (kept locally, not published)
experimental/

# Independent-validation working DATA (master key carries the reference labels →
# never publish; fc/sheets/kml/analysis are bulky local outputs of the R scripts).
# NOTE: the validation SCRIPTS live in validation/ and are versioned; only the
# data folder below is ignored.
validation_data/
